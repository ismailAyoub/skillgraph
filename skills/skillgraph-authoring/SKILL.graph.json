{
  "schemaVersion": 1,
  "doc": {
    "profile": "claude-code",
    "nodes": [
      {
        "id": "entry_root",
        "parentId": null,
        "order": 0,
        "title": "SkillGraph Authoring",
        "provenance": "user",
        "kind": "entry",
        "name": "skillgraph-authoring",
        "description": "Authors and edits Agent Skills as SkillGraph graphs through the skillgraph MCP tools (graph_init, graph_get, graph_apply_patch, graph_lint, graph_compile). Use whenever the user asks to create, write, edit, review, or improve a skill, a SKILL.md, or a SKILL.graph.json, even if they ask to edit the markdown directly, because the graph is the source of truth and SKILL.md is a compiled artifact.",
        "summary": "A SkillGraph skill is a graph. `SKILL.graph.json` holds the nodes and edges; `SKILL.md`, `references/` and `scripts/` are compiled from it. Edit the graph with patches and let the compiler write the markdown.",
        "triggers": [],
        "negativeTriggers": [],
        "allowedTools": [],
        "overview": "auto",
        "referenceIndex": "none"
      },
      {
        "id": "phase_intent",
        "parentId": null,
        "order": 1,
        "title": "Capture the intent",
        "provenance": "user",
        "kind": "phase",
        "summary": "Settle what the skill does and when it should fire, before any node exists.",
        "stepStyle": "numbered"
      },
      {
        "id": "step_scope",
        "parentId": "phase_intent",
        "order": 1,
        "title": "Pin down the workflow",
        "provenance": "user",
        "kind": "step",
        "instruction": "with the user: the trigger, the inputs, the steps a competent person takes, and what \"done\" looks like. Ask about the parts they left implicit rather than inventing them.",
        "why": "A skill that encodes a vague workflow teaches the model a vague workflow."
      },
      {
        "id": "step_description",
        "parentId": "phase_intent",
        "order": 2,
        "title": "Draft the description first",
        "provenance": "user",
        "kind": "step",
        "instruction": "as a third-person \"what + when\" sentence that names the phrases a user would actually type, and make it pushy (\"Use whenever ...\").",
        "why": "Claude decides whether to load a skill from the description alone, so it carries more weight than anything in the body."
      },
      {
        "id": "step_vocab",
        "parentId": "phase_intent",
        "order": 3,
        "title": "Learn the vocabulary",
        "provenance": "user",
        "kind": "step",
        "instruction": "by calling `graph_vocabulary` once per session, so you express the workflow in node kinds instead of prose."
      },
      {
        "id": "phase_open",
        "parentId": null,
        "order": 2,
        "title": "Open or create the graph",
        "provenance": "user",
        "kind": "phase",
        "summary": "Get a `SKILL.graph.json` in front of you before proposing changes.",
        "stepStyle": "numbered"
      },
      {
        "id": "dec_exists",
        "parentId": "phase_open",
        "order": 1,
        "provenance": "user",
        "kind": "decision",
        "question": "Does the skill folder already exist?"
      },
      {
        "id": "step_get",
        "parentId": "phase_open",
        "order": 2,
        "title": "Load the graph",
        "provenance": "user",
        "kind": "step",
        "instruction": "with `graph_get`, passing the folder name as `skill` or an absolute `path`. When the folder holds only a `SKILL.md`, `graph_get` imports it on the fly and reports the coverage; run `graph_import` to make that graph permanent before patching it.",
        "why": "Patching an on-the-fly import without saving it means the next call re-imports and your ids move."
      },
      {
        "id": "step_init",
        "parentId": "phase_open",
        "order": 3,
        "title": "Create the skill",
        "provenance": "user",
        "kind": "step",
        "instruction": "with `graph_init`, giving the kebab-case `name` (it has to equal the folder name) and the description you drafted. It writes the entry node and a first compiled body."
      },
      {
        "id": "phase_patch",
        "parentId": null,
        "order": 3,
        "title": "Propose the structure, then patch it",
        "provenance": "user",
        "kind": "phase",
        "summary": "Agree on the shape in prose, then build it in small reviewable batches.",
        "stepStyle": "numbered"
      },
      {
        "id": "step_outline",
        "parentId": "phase_patch",
        "order": 1,
        "title": "Outline the phases",
        "provenance": "user",
        "kind": "step",
        "instruction": "for the user as a short list before touching the graph: one phase per stage of the workflow, with its steps named underneath.",
        "why": "Re-shaping a graph once thirty nodes exist costs far more than agreeing on four phase titles."
      },
      {
        "id": "step_batch",
        "parentId": "phase_patch",
        "order": 2,
        "title": "Apply one batch at a time",
        "provenance": "user",
        "kind": "step",
        "instruction": "with `graph_apply_patch`: one phase and its steps per call, ops in dependency order, and keep the returned `inverse` patch so you can undo the batch.",
        "why": "A rejected fifty-op patch tells you far less about what went wrong than a rejected six-op patch."
      },
      {
        "id": "step_edges",
        "parentId": "phase_patch",
        "order": 3,
        "title": "Wire the flow",
        "provenance": "user",
        "kind": "step",
        "instruction": "with `next` edges between siblings, two or more `branch` edges out of every decision, and `reads` or `runs` edges from the step that needs a file to the node that emits it.",
        "why": "Edges are what turn the graph into ordered prose instead of an unordered pile of bullets."
      },
      {
        "id": "step_files",
        "parentId": "phase_patch",
        "order": 4,
        "title": "Push long material into references",
        "provenance": "user",
        "kind": "step",
        "instruction": "as `reference` nodes with a `path`, a `body`, a `summary` and a `readWhen`, attached to the step that needs them.",
        "why": "The compiled body is always in context; a reference file is only read when a step says to read it."
      },
      {
        "id": "phase_verify",
        "parentId": null,
        "order": 4,
        "title": "Lint, compile and hand back",
        "provenance": "user",
        "kind": "phase",
        "summary": "Drive the linter to zero errors, compile, and show the user the markdown.",
        "stepStyle": "numbered"
      },
      {
        "id": "loop_fix",
        "parentId": "phase_verify",
        "order": 1,
        "provenance": "user",
        "kind": "loop",
        "until": "`graph_lint` reports zero errors",
        "maxIterations": 5
      },
      {
        "id": "step_lint",
        "parentId": "loop_fix",
        "order": 1,
        "title": "Lint the graph",
        "provenance": "user",
        "kind": "step",
        "instruction": "with `graph_lint`, which checks it against the Agent Skills spec and writes nothing."
      },
      {
        "id": "step_diag",
        "parentId": "loop_fix",
        "order": 2,
        "title": "Read each diagnostic",
        "provenance": "user",
        "kind": "step",
        "instruction": "and find the node it names in `nodeId`."
      },
      {
        "id": "step_fix",
        "parentId": "loop_fix",
        "order": 3,
        "title": "Fix the cause in the graph",
        "provenance": "user",
        "kind": "step",
        "instruction": "with another `graph_apply_patch`. Editing the compiled markdown instead makes the next compile refuse to overwrite your edit."
      },
      {
        "id": "step_compile",
        "parentId": "phase_verify",
        "order": 2,
        "title": "Compile the skill",
        "provenance": "user",
        "kind": "step",
        "instruction": "with `graph_compile`. It writes the markdown and the support files, records their hashes, and returns the line and token report."
      },
      {
        "id": "step_show",
        "parentId": "phase_verify",
        "order": 3,
        "title": "Hand the result back",
        "provenance": "user",
        "kind": "step",
        "instruction": "to the user: the compiled `SKILL.md`, its line count and any remaining lint warnings, so they review the wording rather than the JSON.",
        "why": "The graph is the source of truth, but the markdown is what the model reads at runtime."
      },
      {
        "id": "ref_vocabulary",
        "parentId": null,
        "order": 900,
        "provenance": "user",
        "kind": "reference",
        "path": "references/vocabulary.md",
        "source": "inline",
        "body": "# SkillGraph vocabulary\n\nA skill is a graph: one `entry` node, flow nodes ordered inside containers, file nodes that emit\nsupport files, and edges that sequence, branch, read, run or attach. The compiler turns the graph\ninto a spec-compliant `SKILL.md` plus `references/`, `scripts/` and `assets/`.\n\n## Fields every node has\n\n| Field | Type | Default | Notes |\n|---|---|---|---|\n| `id` | string | required | unique; convention `<kind>_<slug>` (`step_read_diff`, `phase_review`) |\n| `kind` | string | required | one of the 19 kinds below |\n| `parentId` | string or null | `null` | containment; only `phase` and `loop` can be parents |\n| `order` | int | `0` | position among siblings; ties broken by `id` |\n| `title` | string | | bold lead (steps) or heading text (phases) |\n| `provenance` | `user` `import` `ai` | `user` | who authored the node |\n| `overrides` | record | | replace a generated sentence by key (see the spec, section 13) |\n\nCategories: flow kinds (phase, step, decision, loop, ask_user, delegate, skill_call, inject,\nraw_markdown, output_format, catalog, checklist, example, guardrail) render in sequence inside\ntheir container; containers (phase, loop) own children through `parentId`; file kinds\n(reference, script, asset) emit a file and appear in the body only through `reads`/`runs`\nedges; `entry` is the singleton root and `note` is canvas-only.\n\nRoot-section kinds (`output_format`, `example`, `guardrail`, `checklist`, `catalog`) with\n`parentId: null` are collected into synthesized sections (`## Output`, `## Examples`,\n`## Guidelines`, `## Anti-patterns to Avoid`, `## Verification`). Inside a phase they render in\nplace. List-item kinds (`step`, `ask_user`, `delegate`, `skill_call`, `inject`) become items of\nthe surrounding list; everything else renders as a block.\n\n## Node kinds\n\n### entry (exactly one, `id` conventionally `entry_root`)\n`name` (must equal the folder name), `description` (what + when; pushy, third person),\n`title` (H1), `summary` (paragraphs after the H1), `triggers[]`, `negativeTriggers[]` (appended\nas `Do not use for a, b.`), `license`, `compatibility`, `metadata`, `allowedTools[]`,\n`autoAllowScripts`, `claudeCode` (`argumentHint`, `context`, `model`, `disableModelInvocation`,\n`userInvocable`, ...), `overview` (`auto` synthesizes `## How It Works` from root phases; default\n`none`), `usage` (markdown under `## Usage`), `referenceIndex` (`unmentioned` default, `all`,\n`none`), `budget` (`{lines: 500, tokens: 5000}`).\n\n### phase (container, renders a heading)\n`title` (required), `summary` (used by How It Works), `intro` (paragraphs under the heading),\n`stepStyle` (`numbered` default, `bulleted`, `prose`). Root phases are H2; nested phases add a level.\n\n### step (list item)\n`instruction` (markdown; the title is prepended in bold, so write it as the continuation:\ntitle `Read the diff` + instruction `with \\`gh pr diff\\`.`), `why` (trailing rationale sentence),\n`detail[]` (nested bullets), `tools[]` + `mentionTools` (appends `Use X, Y.`).\n\n### decision (block)\n`question`, `intro`. Needs two or more outgoing `branch` edges; each branch's chain of `next`\nsteps renders under the bullet. Branches to a phase or loop render as `go to \"Title\"`.\n\n### loop (container, no heading)\n`until` (stop condition; required in practice), `maxIterations`, `intro`, `why`. Renders\n`Repeat the following until <until> (at most N rounds):` then the children as a list.\n\n### ask_user (list item)\n`question`, `options[]`, `blocking` (default true), `why`. Adds the profile's tool sentence\n(`Use the AskUserQuestion tool ...`).\n\n### reference (file)\n`path` (`references/<name>.md`), `body` (file content), `summary` (`for <summary>`),\n`readWhen` (`when <readWhen>`), `source` (`inline` default, `url` + `url`, `external`),\n`inline` (`never` default; `always` or `auto` <= 12 lines inlines the body into SKILL.md),\n`categoryId`. Attach to a step with a `reads` edge to get\n`Read \\`references/x.md\\` for <summary> when <readWhen>.`\n\n### script (file)\n`path` (`scripts/<name>.sh`), `language`, `code`, `args[]`, `runWhen` (`to <runWhen>`),\n`usage` (emitted as a bash fence), `outputs`. Attach with a `runs` edge. Never executed by SkillGraph.\n\n### asset (file)\n`path`, `content`, `encoding` (`utf8` or `base64`), `usedFor`. Attach with a `reads` edge.\n\n### catalog (block or root section)\n`categories[{id, name, impact, prefix, description}]` generates a priority table; `table` keeps a\nverbatim table; `quickReference: auto` lists member references per category; `intro`.\n\n### output_format (block or `## Output`)\n`template`, `format` (fence language), `strictness` (`exact` or `guide`), `destination`, `intro`.\n\n### guardrail (bullet, or attached paragraph)\n`polarity` (`do` -> Guidelines, `dont` -> Anti-patterns; default `dont`), `text` (bold lead), `why`.\n\n### example (block or `## Examples`)\n`label`, `input`, `output`, `commentary`.\n\n### checklist (root section or block)\n`variant` (`verification`, `red-flags`, `custom`), `style` (`task` or `bullet`),\n`items[{text, why?, checked?}]`, `title`.\n\n### delegate (list item)\n`agentType`, `task`, `parallel`, `returns`. Renders `Spawn an <agentType> subagent with this task: ...`.\n\n### skill_call (list item)\n`skill`, `args`, `when`. Renders `Invoke \\`/skill args\\` when <when>.`\n\n### inject (list item, Claude Code profile only)\n`command`, `label`, `multiline`. Renders `label: !\\`command\\``. Never executed by SkillGraph.\n\n### raw_markdown (block)\n`body`: verbatim markdown escape hatch. Prefer structured kinds; the linter and canvas cannot see inside.\n\n### note (canvas only)\n`body`. Skipped by the compiler.\n\n## Edges\n\n`{ id, kind, source, target, label?, isDefault?, order?, mentioned? }`\n\n| Kind | Source -> target | Effect |\n|---|---|---|\n| `next` | flow node -> flow node, same container | sequence order; inside a loop an edge to a lower `order` is the back-edge |\n| `branch` | decision -> flow node | one labelled branch (`label`; `isDefault` renders `Otherwise`) |\n| `reads` | flow node -> reference or asset | appends the Read/Fetch/Use sentence to the host |\n| `runs` | flow node -> script | appends the Run sentence and the usage fence |\n| `attaches` | guardrail or example -> step or ask_user | renders the source inside the host item |\n\nContainment is `parentId`, not an edge. Give every step in a phase a `next` edge to its\nsuccessor; siblings without edges are flagged `graph/unreachable-node` once any sibling has one.\n`mentioned: true` means the host text already names the file, so no sentence is appended.\n\n## GraphPatch\n\n`graph_apply_patch` takes `{ ops: PatchOp[] }`; ops apply in order, the result is validated, and\nthe inverse patch is returned for undo.\n\n| Op | Payload | Effect |\n|---|---|---|\n| `add` | `{ node }` | append a node (duplicate id fails) |\n| `update` | `{ id, data }` | shallow-merge `data` into the node; omitted keys are kept |\n| `remove` | `{ id }` | remove the node and its edges; children re-parent to its parent |\n| `move` | `{ id, parentId, order }` | re-parent and reorder |\n| `addEdge` | `{ edge }` | append an edge (source and target must exist) |\n| `updateEdge` | `{ id, data }` | shallow-merge edge fields |\n| `removeEdge` | `{ id }` | remove an edge |\n| `setProfile` | `{ profile }` | `universal` or `claude-code` |\n\n```json\n{ \"ops\": [\n  { \"op\": \"add\", \"node\": { \"id\": \"phase_review\", \"kind\": \"phase\", \"parentId\": null, \"order\": 2,\n    \"title\": \"Review\", \"summary\": \"Judge the change.\" } },\n  { \"op\": \"add\", \"node\": { \"id\": \"step_trace\", \"kind\": \"step\", \"parentId\": \"phase_review\", \"order\": 1,\n    \"title\": \"Trace the bug\", \"instruction\": \"to its root cause before judging the fix.\",\n    \"why\": \"A fix that hides the symptom will come back.\" } },\n  { \"op\": \"add\", \"node\": { \"id\": \"step_tests\", \"kind\": \"step\", \"parentId\": \"phase_review\", \"order\": 2,\n    \"title\": \"Check tests\", \"instruction\": \"cover the new behavior.\" } },\n  { \"op\": \"addEdge\", \"edge\": { \"id\": \"e_trace_tests\", \"kind\": \"next\", \"source\": \"step_trace\", \"target\": \"step_tests\" } },\n  { \"op\": \"add\", \"node\": { \"id\": \"ref_standards\", \"kind\": \"reference\", \"parentId\": null, \"order\": 900,\n    \"path\": \"references/standards.md\", \"body\": \"# Standards\\n\\n- No console.log in production.\\n\",\n    \"summary\": \"the team coding standards\", \"readWhen\": \"the diff touches shared code\" } },\n  { \"op\": \"addEdge\", \"edge\": { \"id\": \"e_trace_reads\", \"kind\": \"reads\", \"source\": \"step_trace\", \"target\": \"ref_standards\" } }\n] }\n```\n\nCompiled: `1. **Trace the bug** to its root cause before judging the fix. A fix that hides the\nsymptom will come back. Read \\`references/standards.md\\` for the team coding standards when the diff\ntouches shared code.`\n",
        "summary": "the node kinds, their fields, the edge kinds and the patch ops",
        "readWhen": "choosing a node kind or naming a field",
        "inline": "never"
      },
      {
        "id": "ref_graphpatch",
        "parentId": null,
        "order": 901,
        "provenance": "user",
        "kind": "reference",
        "path": "references/graphpatch.md",
        "source": "inline",
        "body": "# The GraphPatch contract\n\nEvery write to a skill graph goes through `graph_apply_patch`. The tool takes\n`{ skill, patch: { ops: [...] } }`, applies the ops in order, validates the resulting document,\nwrites `SKILL.graph.json`, and (unless you pass `compile: false`) recompiles `SKILL.md` and the\nsupport files. It returns the **inverse patch**: the exact op list that undoes what you just did.\n\n## Contents\n\n- [Rules of the contract](#rules-of-the-contract)\n- [The ops](#the-ops)\n- [Worked examples](#worked-examples)\n- [Batching strategy](#batching-strategy)\n- [When a patch is rejected](#when-a-patch-is-rejected)\n\n## Rules of the contract\n\n- **Ops apply in order.** A node must exist before an edge can point at it, so put every `add`\n  before the `addEdge` that uses it — inside one patch is fine.\n- **The whole patch is atomic.** If any op fails validation, nothing is written and the tool\n  returns an error naming the op.\n- **`update` is a shallow merge.** Keys you omit keep their current value; keys you pass replace\n  it wholesale (an array field is replaced, not appended to).\n- **Ids are yours to choose** and must be unique in the document. Use `<kind>_<slug>`:\n  `phase_review`, `step_read_diff`, `ref_standards`, `e_review_next`.\n- **Containment is `parentId`, not an edge.** Only `phase` and `loop` nodes can be parents.\n- **The entry node's `name` must equal the folder name.** Renaming one means renaming both.\n\n## The ops\n\n| Op | Payload | Effect |\n|---|---|---|\n| `add` | `{ node }` | Append a node. Fails on a duplicate id. |\n| `update` | `{ id, data }` | Shallow-merge `data` into the node. |\n| `remove` | `{ id }` | Remove the node and every edge touching it; its children re-parent to its parent. |\n| `move` | `{ id, parentId, order }` | Re-parent and reorder in one step. |\n| `addEdge` | `{ edge }` | Append an edge. Source and target must already exist. |\n| `updateEdge` | `{ id, data }` | Shallow-merge edge fields (`label`, `isDefault`, `mentioned`, ...). |\n| `removeEdge` | `{ id }` | Remove one edge. |\n| `setProfile` | `{ profile }` | Switch between `universal` and `claude-code`. |\n\n## Worked examples\n\n### Add a phase with two ordered steps\n\n```json\n{ \"ops\": [\n  { \"op\": \"add\", \"node\": { \"id\": \"phase_review\", \"kind\": \"phase\", \"parentId\": null, \"order\": 2,\n    \"title\": \"Review the change\", \"summary\": \"Judge the diff before approving it.\" } },\n  { \"op\": \"add\", \"node\": { \"id\": \"step_trace\", \"kind\": \"step\", \"parentId\": \"phase_review\", \"order\": 1,\n    \"title\": \"Trace the bug\", \"instruction\": \"to its root cause before judging the fix.\",\n    \"why\": \"A fix that only hides the symptom comes back.\" } },\n  { \"op\": \"add\", \"node\": { \"id\": \"step_tests\", \"kind\": \"step\", \"parentId\": \"phase_review\", \"order\": 2,\n    \"title\": \"Check the tests\", \"instruction\": \"cover the new behavior, not just the happy path.\" } },\n  { \"op\": \"addEdge\", \"edge\": { \"id\": \"e_trace_tests\", \"kind\": \"next\",\n    \"source\": \"step_trace\", \"target\": \"step_tests\" } }\n] }\n```\n\n### Attach a reference to a step\n\nThe `reads` edge is what makes the compiler append `Read ... for ... when ...` to the step, and it\nis what stops the linter warning that the file is emitted but never used.\n\n```json\n{ \"ops\": [\n  { \"op\": \"add\", \"node\": { \"id\": \"ref_standards\", \"kind\": \"reference\", \"parentId\": null, \"order\": 900,\n    \"path\": \"references/standards.md\",\n    \"body\": \"# Coding standards\\n\\n- No debug logging in shipped code.\\n\",\n    \"summary\": \"the team coding standards\",\n    \"readWhen\": \"the diff touches shared code\" } },\n  { \"op\": \"addEdge\", \"edge\": { \"id\": \"e_trace_reads\", \"kind\": \"reads\",\n    \"source\": \"step_trace\", \"target\": \"ref_standards\" } }\n] }\n```\n\n### Branch on a decision\n\nA `decision` needs two or more outgoing `branch` edges. The chain of `next` steps hanging off each\nbranch target renders underneath that branch.\n\n```json\n{ \"ops\": [\n  { \"op\": \"add\", \"node\": { \"id\": \"dec_size\", \"kind\": \"decision\", \"parentId\": \"phase_review\", \"order\": 0,\n    \"question\": \"Is the diff larger than 400 lines?\" } },\n  { \"op\": \"addEdge\", \"edge\": { \"id\": \"e_big\", \"kind\": \"branch\", \"source\": \"dec_size\",\n    \"target\": \"step_split\", \"label\": \"It is larger\" } },\n  { \"op\": \"addEdge\", \"edge\": { \"id\": \"e_small\", \"kind\": \"branch\", \"source\": \"dec_size\",\n    \"target\": \"step_trace\", \"isDefault\": true } }\n] }\n```\n\n### Edit, reorder and delete\n\n```json\n{ \"ops\": [\n  { \"op\": \"update\", \"id\": \"step_tests\",\n    \"data\": { \"why\": \"Untested branches are where regressions hide.\" } },\n  { \"op\": \"move\", \"id\": \"step_tests\", \"parentId\": \"phase_review\", \"order\": 1 },\n  { \"op\": \"updateEdge\", \"id\": \"e_trace_reads\", \"data\": { \"mentioned\": true } },\n  { \"op\": \"removeEdge\", \"id\": \"e_trace_tests\" },\n  { \"op\": \"remove\", \"id\": \"step_split\" }\n] }\n```\n\n### Fill in the entry node\n\nFrontmatter lives on the `entry` node, so metadata edits are ordinary `update` ops.\n\n```json\n{ \"ops\": [\n  { \"op\": \"update\", \"id\": \"entry_root\", \"data\": {\n    \"description\": \"Reviews a pull request diff for correctness and test coverage. Use whenever the user asks for a code review, a diff review, or a second pair of eyes before merging.\",\n    \"summary\": \"Reads the diff, traces each change to its cause, and reports findings by severity.\",\n    \"overview\": \"auto\",\n    \"budget\": { \"lines\": 400, \"tokens\": 4000 } } }\n] }\n```\n\n## Batching strategy\n\nOne phase and its steps per call. A batch that size is small enough to read in the response, small\nenough to undo with the returned `inverse` patch, and large enough that the graph is never left\nwith a phase that has no steps in it.\n\nDo not send one op per call: the graph is validated and recompiled on every call, so a fifty-call\nsequence is fifty compiles and fifty chances to leave the graph in a half-built state.\n\n## When a patch is rejected\n\nThe tool returns an error instead of writing when:\n\n- **The JSON does not match the schema** — the message names the op index and the offending field.\n  Check the node kind's field table in `references/vocabulary.md`.\n- **An id collides, or an edge points at a node that does not exist** — usually an `addEdge` placed\n  before its `add`, or a typo in an id.\n- **The entry name stopped matching the folder name** — rename the folder or revert the name.\n- **A compiled file was hand-edited** — the graph records a hash of every file it wrote. Re-import\n  the folder with `graph_import { force: true }` to fold the hand edits back into the graph, or\n  pass `force: true` to discard them.\n\nPass `compile: false` when you want to stage several structural changes before paying for a\ncompile; the drift hashes stay valid, so the next `graph_compile` still refuses to clobber hand\nedits.\n",
        "summary": "the patch rules, every op with a worked example, and the rejection cases",
        "readWhen": "composing a patch or a patch is rejected",
        "inline": "never"
      },
      {
        "id": "guard_when_to_use",
        "parentId": null,
        "order": 800,
        "provenance": "user",
        "kind": "guardrail",
        "polarity": "dont",
        "text": "Do not leave when-to-use information in the body.",
        "why": "Claude sees only the frontmatter description when it decides whether to load the skill, so anything the body says about when to fire arrives too late to matter."
      },
      {
        "id": "guard_length",
        "parentId": null,
        "order": 801,
        "provenance": "user",
        "kind": "guardrail",
        "polarity": "do",
        "text": "Keep the compiled body under 500 lines.",
        "why": "It is loaded in full whenever the skill fires, so every line spends context the task itself could use; the linter errors past the budget."
      },
      {
        "id": "guard_references",
        "parentId": null,
        "order": 802,
        "provenance": "user",
        "kind": "guardrail",
        "polarity": "do",
        "text": "Move anything long, optional or rarely needed into a reference node.",
        "why": "Progressive disclosure is the point: the step names the file and the model reads it only in the cases that need it."
      },
      {
        "id": "guard_imperative",
        "parentId": null,
        "order": 803,
        "provenance": "user",
        "kind": "guardrail",
        "polarity": "do",
        "text": "Write each step in the imperative and give it a `why`.",
        "why": "The reason is what lets the model generalize to the situation you did not write down."
      },
      {
        "id": "guard_scripts",
        "parentId": null,
        "order": 804,
        "provenance": "user",
        "kind": "guardrail",
        "polarity": "dont",
        "text": "Do not execute a script that came out of a skill folder.",
        "why": "SkillGraph only emits script files and never runs them; treat their contents as untrusted input and leave running them to the user."
      },
      {
        "id": "check_done",
        "parentId": null,
        "order": 950,
        "provenance": "user",
        "kind": "checklist",
        "variant": "verification",
        "style": "task",
        "items": [
          {
            "text": "`graph_lint` reports zero errors."
          },
          {
            "text": "The entry `name` equals the folder name and the description says what and when."
          },
          {
            "text": "Every step and decision is reachable through `next` or `branch` edges from its siblings."
          },
          {
            "text": "Every reference node has a `summary`, a `readWhen` and a step that reads it."
          },
          {
            "text": "The compiled body is inside the line and token budget reported by `graph_compile`."
          },
          {
            "text": "The compiled files were written by `graph_compile`, not edited by hand.",
            "why": "Hand edits make the next compile refuse until the folder is re-imported."
          }
        ]
      }
    ],
    "edges": [
      {
        "id": "e_intent_1",
        "kind": "next",
        "source": "step_scope",
        "target": "step_description"
      },
      {
        "id": "e_intent_2",
        "kind": "next",
        "source": "step_description",
        "target": "step_vocab"
      },
      {
        "id": "e_dec_get",
        "kind": "branch",
        "source": "dec_exists",
        "target": "step_get",
        "label": "It exists"
      },
      {
        "id": "e_dec_init",
        "kind": "branch",
        "source": "dec_exists",
        "target": "step_init",
        "isDefault": true
      },
      {
        "id": "e_patch_1",
        "kind": "next",
        "source": "step_outline",
        "target": "step_batch"
      },
      {
        "id": "e_patch_2",
        "kind": "next",
        "source": "step_batch",
        "target": "step_edges"
      },
      {
        "id": "e_patch_3",
        "kind": "next",
        "source": "step_edges",
        "target": "step_files"
      },
      {
        "id": "e_loop_1",
        "kind": "next",
        "source": "step_lint",
        "target": "step_diag"
      },
      {
        "id": "e_loop_2",
        "kind": "next",
        "source": "step_diag",
        "target": "step_fix"
      },
      {
        "id": "e_verify_1",
        "kind": "next",
        "source": "loop_fix",
        "target": "step_compile"
      },
      {
        "id": "e_verify_2",
        "kind": "next",
        "source": "step_compile",
        "target": "step_show"
      },
      {
        "id": "e_vocab_reads",
        "kind": "reads",
        "source": "step_vocab",
        "target": "ref_vocabulary"
      },
      {
        "id": "e_batch_reads",
        "kind": "reads",
        "source": "step_batch",
        "target": "ref_graphpatch"
      }
    ]
  },
  "layout": {
    "nodes": {},
    "viewport": {
      "x": 0,
      "y": 0,
      "zoom": 1
    }
  },
  "compiled": {
    "profile": "claude-code",
    "at": "2026-09-02T16:11:16.446Z",
    "files": {
      "SKILL.md": "01948a9f59a7bdba",
      "references/vocabulary.md": "4712df84e2c6b3b4",
      "references/graphpatch.md": "8b58f689aa48d6e8"
    }
  }
}
